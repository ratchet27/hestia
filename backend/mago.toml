# Welcome to Mago!
# For full documentation, see https://mago.carthage.software/tools/overview
php-version = "8.4"

[source]
workspace = "."
paths = ["src", "tests"]
includes = ["vendor"]
excludes = ["src/Kernel.php", "tests/bootstrap.php"]

[formatter]
preset = "psr-12"
print-width = 120
tab-width = 4
use-tabs = false

[linter]
integrations = ["symfony", "phpunit"]

[linter.rules]
ambiguous-function-call = { enabled = false }
literal-named-argument = { enabled = false }
# `isset` is the null-safe idiom for optional query filters (ProductRepository); array_key_exists would change null-handling
no-isset = { enabled = false }
halstead = { effort-threshold = 7000 }
too-many-methods = { threshold = 20 }

[analyzer]
find-unused-definitions = true
find-unused-expressions = true
analyze-dead-code = true
memoize-properties = true
allow-possibly-undefined-array-keys = true
check-throws = false
check-missing-override = false
find-unused-parameters = true
strict-list-index-checks = true
no-boolean-literal-comparison = true
check-missing-type-hints = true
register-super-globals = true
excludes = ["tests", "src/Story"]
