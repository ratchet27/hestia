# API base URL for fetch requests. Leave empty (default): the API is same-origin,
# via the Vite dev proxy in dev and Caddy in prod. Set only if the API is hosted
# on a different origin, e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
